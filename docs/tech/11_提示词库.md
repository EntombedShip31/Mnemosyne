# 11 · 提示词库（可直接复制）

> 配套 `10_多线程开发规范.md`。
> 本文件里的每个提示词都是**自包含**的——复制过去就能用，不需要额外解释。
> **本项目根包名已冻结为 `com.etbs31.mnemosyne`**（Phase 0 定稿），
> 提示词里的 `<author>` 一律替换为 `etbs31`，不要再改动。

---

## 〇、怎么用

```
1. 打开 docs/tech/10_多线程开发规范.md §二，找到当前 Phase
2. 从本文件里复制对应工作流的提示词
3. 每个工作流开一个独立的会话，粘贴对应的提示词
4. 同一个 Phase 的所有会话可以**同时**跑
5. Phase 内的所有工作流完成后，用 §十三 的集成检查点提示词收口
```

---

## 一、通用模板（自己写新任务时用）

```
【任务】
<一句话，不超过两行>

【必读】
1. docs/tech/10_多线程开发规范.md §〇（三条铁律）§一（文件所有权表）
2. docs/tech/<x>_<名>.md §<节>  —— <解决什么问题>
3. docs/tech/<y>_<名>.md §<节>  —— <解决什么问题>
4. docs/<z>_<名>.md            —— <设计意图>

【你拥有的文件】（只许新建/修改以下文件）
- src/main/java/com/<author>/mnemosyne/<path>/<File>.java
- src/main/resources/<path>

【接口契约】（已冻结，不可改；要改先停下来申请）
- <Class>.<method>(<args>) : <ret>
- NBT 键：<key>
- 注册 ID：mnemosyne:<id>

【实现要求】
1. <...>
2. <...>

【验收标准】（逐条自测并汇报）
1. ./gradlew build 通过
2. <游戏内验证步骤>
3. <预期现象>

【禁止】
- 修改 docs/tech/01_ISS官方API参考.md 的任何结论
- 修改其他工作流拥有的文件
- new ResourceLocation(...)（用 ResourceLocation.fromNamespaceAndPath / parse）
- 在 FMLCommonSetupEvent 里直接注册 ISS 自定义注册表（必须 event.enqueueWork）
- 新增 .ogg 文件

【汇报格式】
见 docs/tech/10_多线程开发规范.md §八
```

---

## 二、Phase 0 · WS-0 基础设施

```
【任务】
从零搭建忆海（Mnemosyne）模组的可编译、可启动的空壳工程。

【必读】
1. docs/tech/07_1.20.1Forge模组开发指南.md §2 §3 §4 §5 §十  —— 逐字可用的配置与 8 个坑
2. docs/tech/00_技术总览.md §目标环境 §依赖矩阵

【你拥有的文件】（全部新建）
- build.gradle
- gradle.properties
- settings.gradle
- src/main/resources/META-INF/mods.toml
- src/main/resources/pack.mcmeta
- src/main/java/com/<author>/mnemosyne/MnemosyneMod.java
- src/main/java/com/<author>/mnemosyne/Config.java

【硬性参数】
- MC 1.20.1 / Forge 47.4.0 / Java 17 / mappings: official 1.20.1
- mod_id = mnemosyne
- pack_format = 15
- 依赖：irons_spellbooks 1.20.1-3.16.3 (api + full)、irons_lib 1.20.1-2.1.0 (runtimeOnly)、
        curios 5.14.1+1.20.1、geckolib 1.20.1:4.7.1.1、player-animator 1.0.2-rc1+1.20、caelus 3.1.0+1.20
- mods.toml 里 irons_spellbooks 和 curios 必须 ordering="AFTER"
- ⚠️ curios 必须 ≥ 5.14.1+1.20.1，且必须显式加 irons_lib（ISS 的 POM 里没有任何依赖声明）。
  否则 ./gradlew build 会成功，但 runClient 崩在 ModSorter —— 见 docs/tech/07 §十 坑 #9

【实现要求】
1. gradle.properties 只放 ASCII（它是 ISO-8859-1）
2. jar 文件名必须 ASCII：base { archivesName = "mnemosyne" }
3. 三个前置（geckolib / player-animator / caelus）都要加，否则 runClient 崩溃
4. 主类里预留 registerConfig + ModSounds/ModItems/ModAttributes 的注册调用（可以先注释掉）
5. 主类里预留 onCommonSetup(FMLCommonSetupEvent) 并在里面用 event.enqueueWork(...)

【验收标准】
1. ./gradlew build 成功
2. ./gradlew runClient 能进主菜单
3. 模组列表显示 "忆海 · Mnemosyne"
4. 日志里搜 "mnemosyne" 无 ERROR

【禁止】
- 不要创建任何 registry/ spell/ capability/ 包（那是后续工作流的）
- 不要引用任何还不存在的类
```

---

## 三、Phase 1 · WS-A 注册层（关键路径，优先启动）

```
【任务】
注册忆海学派、两个属性、忆晶物品、音效事件，以及 16 个法术的注册行（指向 stub 类）。

【必读】
1. docs/tech/10_多线程开发规范.md §〇 §一
2. docs/tech/02_学派与属性注册.md  —— 全文，这是你的主文档
3. docs/tech/01_ISS官方API参考.md §SchoolType §包可见性  —— 查构造参数
4. docs/tech/04_法术等级强度表.md §总表  —— 拿到 16 个法术的 spellId / minRarity / maxLevel

【你拥有的文件】
- src/main/java/com/<author>/mnemosyne/registry/ModAttributes.java
- src/main/java/com/<author>/mnemosyne/registry/ModSchools.java
- src/main/java/com/<author>/mnemosyne/registry/ModItems.java
- src/main/java/com/<author>/mnemosyne/registry/ModSounds.java
- src/main/java/com/<author>/mnemosyne/registry/ModSpells.java
- src/main/java/com/<author>/mnemosyne/spell/low/*.java      ← 只建 stub
- src/main/java/com/<author>/mnemosyne/spell/mid/*.java      ← 只建 stub
- src/main/java/com/<author>/mnemosyne/spell/high/*.java     ← 只建 stub
- src/main/resources/assets/mnemosyne/lang/en_us.json
- src/main/resources/assets/mnemosyne/lang/zh_cn.json
- src/main/resources/data/mnemosyne/damage_type/memory.json
- src/main/resources/assets/mnemosyne/models/item/*.json

【接口契约】（冻结，下游全部依赖这些）
- 学派 ID：mnemosyne:memory
- 强度属性：mnemosyne:memory_spell_power（默认 1.0）
- 抗性属性：mnemosyne:memory_magic_resist（默认 0.0）
- 伤害类型：mnemosyne:memory
- Focus 标签：mnemosyne:memory_focus
- 忆晶物品：mnemosyne:memory_crystal
- 音效事件：见 src/main/resources/assets/mnemosyne/sounds.json（已定稿，39 个事件，不要改）
- 16 个法术 ID 与类名：见 docs/tech/04_法术等级强度表.md §总表
- 每个法术类必须 extends 对应基类（基类由 WS-C 提供，先写 stub 让编译通过）

【实现要求】
1. 属性用 ForgeRegistries.ATTRIBUTES 的 DeferredRegister
2. 学派用 SchoolRegistry.SCHOOL_REGISTRY_KEY + DeferredRegister
3. 学派注册必须放在 FMLCommonSetupEvent 的 event.enqueueWork(...) 里
4. 法术注册同理，放在 enqueueWork 里
5. ModSounds 注册 sounds.json 里的全部 39 个事件
6. lang 文件里要包含：学派名、16 个法术名+描述、忆晶、全部 subtitles.mnemosyne.*
7. 每个法术先建一个能编译的 stub：构造器调 super(getDefaultConfig())，四个必需覆写返回占位值
8. stub 里加 TODO 注释指向对应的工作流（WS-D1/D2/D3）

【验收标准】
1. ./gradlew build 通过
2. /attribute @s mnemosyne:memory_spell_power get 返回 1.0
3. 创造模式物品栏能看到"忆晶"
4. 16 个法术全部注册成功（用 /irons_spellbooks 相关指令或日志确认）
5. 学派在 SchoolRegistry 里能查到 mnemosyne:memory

【禁止】
- 不要实现任何法术的实际逻辑（那是 WS-D 的活）
- 不要改 sounds.json
- 不要创建 capability/ network/ client/ oblivion/ block/ entity/ 包
```

---

## 四、Phase 1 · WS-H 美术资源

```
【任务】
生成忆海的全部物品/方块贴图。

【必读】
1. docs/tech/10_多线程开发规范.md §〇 §一
2. docs/tech/09_美术资源管线.md  —— 全文，这是你的主文档
3. docs/09_美术与音效.md §配色系统  —— 设计意图

【你拥有的文件】
- src/main/resources/assets/mnemosyne/textures/**

【配色契约】（冻结）
- 靛蓝 #534AB7（主色，用于文字/符文）
- 品红 #D4537E（辅色，用于封印/绑带）
- 卷轴纸面：淡薰衣草系（#EFEDF7 / #B7B0D4 / #5C5480）

【任务清单】
已完成（脚本生成，不要重做）：
- ✅ textures/item/scroll_memory.png
- ✅ textures/item/memory_rune.png

待做：
1. textures/item/memory_crystal.png          16x16  忆晶   ← ⚠️ 最优先，现在游戏里是紫黑格
2. textures/item/memory_stele_fragment.png   16x16  忆碑碎片
3. textures/item/mnemonic_robe_helmet.png    16x16  护甲物品图标
4. textures/item/mnemonic_robe_chestplate.png 16x16
5. textures/item/mnemonic_robe_leggings.png  16x16
6. textures/item/mnemonic_robe_boots.png     16x16
7. textures/models/armor/mnemonic_layer_1.png 64x32 穿戴图层（头/胸/靴）
8. textures/models/armor/mnemonic_layer_2.png 64x32 穿戴图层（腿）
9. textures/block/mnemonic_bricks.png        16x16  遗迹建材
10. textures/block/mnemonic_bricks_cracked.png 16x16 裂纹变体
11. textures/block/memory_stele.png          16x16  忆碑
12. textures/block/memory_crystal_cluster.png 16x16 忆晶簇

⭐ 13. **法术图标 16 张**（16x16）—— 原清单漏了这一项，2026-09-16 补
   路径：assets/mnemosyne/textures/gui/spell_icons/<spellId>.png
   实测依据：AbstractSpell.getSpellIconResource() 返回
     assets/<namespace>/textures/gui/spell_icons/<getSpellName()>.png
   ISS 自己有 116 张同类图标（assets/irons_spellbooks/textures/gui/spell_icons/），
   我们一张都没有 → 法术书里 16 个忆海法术的图标全部是缺失贴图。
   16 个 id：memory_arrow / glimpse / memory_shard / encode_trait / encode_pain / engrave /
             amnesia / expand_mind / recollection / curse_of_oblivion / cognitive_collapse /
             memory_theft / mass_amnesia / deja_vu / sea_of_memory / thousand_memories
   ⚠️ 已删除，不要再加回来：forget（2026-09-18，与 amnesia 重复）、encode_spell（术忆）、
      recite（复诵）。下面这份清单是**当时的快照**，其余内容按快照读即可。

（合计 30 张）

【实现要求】
1. ⭐ 用 `tools/gen_mnemosyne_art.py`（WS-H2 新建，2026-09-16）：
   `--all` 生成全部 30 张 / `--check` 逐张校验 / `--contact-sheet <path>` 出拼版预览图
   卷轴与符文仍用 `tools/gen_scroll_texture.py`（`--mode scroll` / `ramp`）
2. 方块贴图**程序绘制**（错缝砖 + 噪声，可四方连续）。原版 `deepslate_bricks` 贴图本地没有
   （`forge_gradle/assets/indexes/5.json` 只含声音不含贴图），所以不依赖原版资源改色
3. 所有贴图必须是 PNG，尺寸精确，文件名全小写
4. 生成后必须肉眼检查（无紫黑格、无接缝异常、明暗关系正确）

【验收标准】
1. 30 张贴图全部存在且尺寸正确（用 Python 逐张断言尺寸）
2. 用 Python 校验：每张 PNG 能打开、无全透明
3. 文件名与 18 个 spellId 逐字一致（脚本比对文件名集合）
4. 在游戏里（如果 WS-A 已完成）确认无紫黑格 —— **需要 runClient，静态检查替代不了**

【禁止】
- 不要把 ISS 的官方贴图直接复制过来（ISS 是 All Rights Reserved）
- 不要改 assets/mnemosyne/sounds.json（那是 WS-A 的）
- 不要改 models/item/*.json（那是 WS-A 的）
```

---

## 五、Phase 1 · WS-G1 世界生成（JSON 部分）

```
【任务】
编写忆者遗迹（memory_ruin）的全部世界生成 JSON。

【必读】
1. docs/tech/10_多线程开发规范.md §〇 §一
2. docs/tech/06_世界生成.md  —— 全文，这是你的主文档（含 pyromancer_tower 逐字模板）
3. docs/08_建筑_忆者遗迹.md  —— 设计意图、四区布局、战利品设计

【你拥有的文件】
- src/main/resources/data/mnemosyne/worldgen/structure/memory_ruin.json
- src/main/resources/data/mnemosyne/worldgen/structure_set/memory_ruin.json
- src/main/resources/data/mnemosyne/worldgen/template_pool/memory_ruin/*.json
- src/main/resources/data/mnemosyne/worldgen/processor_list/memory_ruin_randomization.json
- src/main/resources/data/mnemosyne/tags/worldgen/biome/has_structure/memory_ruin.json
- src/main/resources/data/mnemosyne/loot_tables/chests/memory_ruin/*.json

【硬性参数】（冻结）
- 结构 ID：mnemosyne:memory_ruin
- salt：1852304719（已确认与 ISS 全部结构不冲突）
- spacing：64，separation：40
- step：underground_decoration
- terrain_adaptation：beard_thin（地表）/ bury（地下）
- max_distance_from_center：96
- 群系：#minecraft:is_forest / is_taiga / is_jungle / #forge:is_plains / is_hill / is_mountain
- 池元素类型：irons_spellbooks:individual_terrain_element（直接复用 ISS 的，不要自己注册）

【实现要求】
1. 直接对照 docs/tech/06_世界生成.md 里 pyromancer_tower 的原文结构写
2. 模板池要有三层 fallback 链（chamber_pool → chamber_fallback → minecraft:empty）
3. 战利品表必须用官方函数：
   { "function": "irons_spellbooks:randomize_spell",
     "quality": {...}, "spell_filter": { "school": "mnemosyne:memory" } }
4. 处理器列表里加忆海专属建材 mnemosyne:mnemonic_bricks 的风化规则
5. 所有 JSON 必须是合法 JSON（不要用 // 注释，除非确认加载器支持）

【验收标准】
1. 全部 JSON 用 python -m json.tool 校验通过
2. 结构文件引用的每个 nbt 路径都在文档里登记（供 WS-G2 制作）
3. （如果游戏能跑）/locate structure mnemosyne:memory_ruin 返回坐标

【禁止】
- 不要制作 .nbt 文件（那是 WS-G2 的，必须在游戏里用结构方块做）
- 不要用 irons_lib:randomize_player_statue（依赖 ISS 内部库）
- 不要改 sounds.json / lang / 任何 Java 文件
```

---

## 六、Phase 2 · WS-B 忆格系统

```
【任务】
实现忆格（Engram）数据层：存储、腐坏、共鸣计算、网络同步。

【必读】
1. docs/tech/10_多线程开发规范.md §〇 §一
2. docs/tech/03_法术实现规范.md §六（忆格系统）§九（网络同步）  —— 你的主文档
3. docs/01_核心机制_忆格系统.md  —— 设计意图、边界情况、误设计清单

【你拥有的文件】
- src/main/java/com/<author>/mnemosyne/capability/MnemosyneData.java
- src/main/java/com/<author>/mnemosyne/capability/EngramEntry.java
- src/main/java/com/<author>/mnemosyne/capability/EngramType.java
- src/main/java/com/<author>/mnemosyne/network/NetworkHandler.java
- src/main/java/com/<author>/mnemosyne/network/packet/S2CSyncEngramsPacket.java

【接口契约】（冻结，WS-D/WS-E/WS-I 全部依赖）
- MnemosyneData.getUsedEngrams(Player) : int
- MnemosyneData.getMaxEngrams(Player) : int        // 基础 3，上限 5
- MnemosyneData.addEngram(Player, EngramEntry) : boolean
- MnemosyneData.releaseEngram(Player, int index) : EngramEntry
- MnemosyneData.clearAll(Player) : void
- MnemosyneData.getResonanceMultiplier(Player) : double   // 1 + 0.20 * used
- MnemosyneData.getManaPenalty(Player) : double           // 1 + 0.10 * used
- MnemosyneData.getCastSpeedPenalty(Player) : double      // 1 - 0.08 * used
- MnemosyneData.tick(ServerPlayer) : void
- EngramType 枚举：SPELL / ESSENCE / PAIN

【实现要求】
1. ⚠️ 1.20.1 Forge 没有 AttachmentType，用 player.getPersistentData() 存 NBT
2. NBT 结构按 docs/tech/03_法术实现规范.md §6.2 的约定
3. 腐坏计时：120 秒（2400 tick），到期自动移除
4. 玩家死亡时清空全部忆格（订阅 PlayerEvent.Clone 的 death 分支，用 event.isWasDeath()）
5. 网络同步用脏标记，不要每 tick 发包
6. tick() 每 20 tick 检查一次腐坏，不要每 tick 全量扫描
7. 共鸣/惩罚的数值必须是纯函数，方便 WS-D 直接调用

【验收标准】
1. ./gradlew build 通过
2. 手动写入一个假忆格后 /data get entity @s 能看到 NBT
3. 120 秒后自动消失
4. 死亡后清空
5. 重新登录后忆格还在（持久化正确）

【禁止】
- 不要用 AttachmentType（不存在于 1.20.1）
- 不要改 spell/ 下的任何文件
- 不要实现 HUD 渲染（那是 WS-I 的）
```

---

## 七、Phase 2 · WS-C 法术基类

> **✅ 已完成（2026-09-15）**，提示词保留供追溯。实际交付 8 个文件
> （比原清单多了 `MnemosyneSpell.java` 公共父类与 `BaseContractSelfTest.java` 契约自检），
> 实现细节与新发现的坑见 `03_法术实现规范.md` §3.5~§3.7。
>
> **执行中发现并已修正的两处**：
> ① `AbstractSpell` **只有无参构造器**，原骨架里的 `super(config)` 编译不过；
> ② `compileOnly :api` 分类器**编译不过任何造成伤害的法术**（缺 `SpellDamageSource`），
> 已改为编译期用 ISS 完整 jar —— 见 `00_技术总览.md` §二、`build.gradle` 注释。

```
【任务】
实现四个法术基类 + 长吟计时器。

【必读】
1. docs/tech/10_多线程开发规范.md §〇 §一
2. docs/tech/03_法术实现规范.md §一 §二 §三 §四 §五  —— 你的主文档
3. docs/tech/01_ISS官方API参考.md §AbstractSpell  —— 方法签名基准

【你拥有的文件】
- src/main/java/com/<author>/mnemosyne/spell/base/EncodeSpell.java
- src/main/java/com/<author>/mnemosyne/spell/base/OblivionSpell.java
- src/main/java/com/<author>/mnemosyne/spell/base/MnemosyneLongCastSpell.java
- src/main/java/com/<author>/mnemosyne/spell/base/MnemosyneProjectileSpell.java
- src/main/java/com/<author>/mnemosyne/util/LongCastTracker.java
- src/main/java/com/<author>/mnemosyne/util/RaycastHelper.java

【接口契约】（冻结，WS-D1/D2/D3 全部继承这些）
- EncodeSpell(DefaultConfig)                     // 写入类基类
  protected abstract void onEncode(ServerPlayer, LivingEntity target, int spellLevel)
- OblivionSpell(DefaultConfig)                   // 遗忘类基类
  protected abstract int getOblivionTier()
  protected abstract void applyOblivion(ServerPlayer, LivingEntity target, int spellLevel)
- MnemosyneLongCastSpell(DefaultConfig)          // 长吟类基类
  @Override public CastType getCastType() → LONG
  protected abstract int defaultCastTime()
  protected abstract void onLongCastTick(ServerPlayer, int spellLevel, float progress, MagicData)
  protected abstract void onLongCastFinish(ServerPlayer, int spellLevel)
- MnemosyneProjectileSpell(DefaultConfig)        // 投射物基类
  protected abstract float getProjectileDamage(int spellLevel)
- LongCastTracker.begin(ServerPlayer) / elapsed(ServerPlayer) : int / end(ServerPlayer)

【实现要求】
1. 伤害必须走 getDamageSource() 返回的 SpellDamageSource，不要用原版 DamageSource
2. 长吟进度用 LongCastTracker 自计时（player.tickCount 差值），不要反射读 MagicData 私有字段
3. 长吟被打断的处理：SpellPreCastEvent 配对清理 + PlayerTickEvent 超时兜底（见 03 §3.4）
4. 投射物自己写（继承 Projectile），实现 api.entity.NoKnockbackProjectile
5. 所有基类都要能被子类正常实例化（写一个临时 stub 子类验证编译）

【验收标准】
1. ./gradlew build 通过
2. 四个基类各有一个临时 stub 子类能编译
3. 一个 LONG 类型的测试法术能吟唱 2 秒后触发 onLongCastFinish
4. 中途被打断时 onLongCastFinish 不触发、且状态被正确清理

【禁止】
- 不要实现具体的 16 个法术（那是 WS-D 的）
- 不要改 spell/low|mid|high/ 下的文件
- 不要改 registry/ 下的文件
```

---

## 八、Phase 3 · WS-D1 低阶法术（8 个）

```
【任务】
实现 8 个低阶法术（Common 4 + Uncommon 4）。

【必读】
1. docs/tech/10_多线程开发规范.md §〇 §一
2. docs/tech/04_法术等级强度表.md §法术 1-8  —— 5 个等级的精确数值
3. docs/tech/03_法术实现规范.md §3.1 §3.2  —— 基类用法
4. docs/04_法术_低阶_Common与Uncommon.md  —— 设计意图

【你拥有的文件】
- src/main/java/com/<author>/mnemosyne/spell/low/*.java   （6 个文件 · 快照值）

【法术清单与基类】
| 法术 | 类名 | 继承 |
|---|---|---|
| 忆矢 | MemoryArrowSpell | MnemosyneProjectileSpell |
| 窥忆 | GlimpseSpell | AbstractSpell |
| ~~遗忘~~ | ~~ForgetSpell~~ **已删除** | ~~OblivionSpell~~ |
| 碎忆 | MemoryShardSpell | AbstractSpell |
| 写入·术忆 | EncodeSpellSpell | EncodeSpell |
| 写入·质忆 | EncodeTraitSpell | EncodeSpell |
| 写入·痛忆 | EncodePainSpell | EncodeSpell |
| 失忆 | AmnesiaSpell | OblivionSpell（2026-09-18 挪到 `spell/mid/`） |

【硬性数值】
严格照抄 docs/tech/04_法术等级强度表.md 里每个法术的：
baseManaCost / manaCostPerLevel / baseSpellPower / spellPowerPerLevel / castTime / cooldown / maxLevel / minRarity

【实现要求】
1. 每个类只实现自己独有的逻辑，公共逻辑走基类
2. 音效按 docs/tech/08_音效方案.md §3.2 的表调用 ModSounds
3. 冷却用 getSpellCooldown()（固定值，不随等级变）
4. 需要随等级变的施法时间必须覆写 getCastTime(int level)
5. 忆矢的投射物用 WS-C 的 MnemosyneProjectileSpell
6. 写入类法术要调 MnemosyneData.addEngram(...)

【验收标准】
逐条给出证据：
1. ./gradlew build 通过
2. 8 个法术都能通过卷轴施放
3. 忆矢 5 级的伤害符合 04 号文档的表
4. 写入类法术成功后忆格 +1（用 /data get 验证）
5. 音效正常播放

【禁止】
- 不要改基类（需要新方法就走接口变更申请）
- 不要改 registry/ModSpells.java（注册行已由 WS-A 写好）
- 不要改 spell/mid|high/
```

---

## 九、Phase 3 · WS-D2 中阶法术（4 个）

```
【任务】
实现 4 个中阶（Rare）法术。

【必读】
1. docs/tech/10_多线程开发规范.md §〇 §一
2. docs/tech/04_法术等级强度表.md §法术 9-12
3. docs/tech/03_法术实现规范.md §3.1 §3.4
4. docs/05_法术_中阶_Rare.md

【你拥有的文件】
- src/main/java/com/<author>/mnemosyne/spell/mid/*.java   （4 个文件）

【法术清单】
| 法术 | 类名 | 继承 | 备注 |
|---|---|---|---|
| 忆格扩张 | ExpandMindSpell | AbstractSpell | 临时忆格 +2，30 秒 |
| 复诵 | ReciteSpell | AbstractSpell | 释放指定忆格且不消耗 |
| 走马灯 | RecollectionSpell | AbstractSpell | 复现目标最近技能 |
| 遗忘诅咒 | CurseOfOblivionSpell | OblivionSpell | 领域，tier 2 |

【接口契约】
- 复诵需要"选择忆格索引"：用网络包从客户端传索引，服务端校验
- 临时忆格用 MnemosyneData 的临时槽位（见 03 §6.7）

【验收标准】
1. ./gradlew build 通过
2. 4 个法术能施放，数值符合 04 号文档
3. 复诵能指定索引且不消耗忆格
4. 忆格扩张的临时忆格 30 秒后消失

【禁止】
- 不要改基类 / registry / spell/low|high/
```

---

## 十、Phase 3 · WS-E 遗忘系统（工作量最大）

```
【任务】
实现遗忘系统：AI 行为移除、特性窃取、BOSS 免疫与降级。

【必读】
1. docs/tech/10_多线程开发规范.md §〇 §一
2. docs/tech/03_法术实现规范.md §七 §八  —— 你的主文档，含 7.3 的核心难题
3. docs/02_核心机制_遗忘与能力映射.md  —— 三张映射表（32 生物 / 25 特性 / 18 技能）

【你拥有的文件】
- src/main/java/com/<author>/mnemosyne/oblivion/OblivionManager.java
- src/main/java/com/<author>/mnemosyne/oblivion/AbilityMap.java
- src/main/java/com/<author>/mnemosyne/oblivion/TraitRegistry.java
- src/main/java/com/<author>/mnemosyne/oblivion/OblivionTier.java
- src/main/java/com/<author>/mnemosyne/oblivion/BossImmunity.java

【接口契约】（冻结，WS-D 依赖）
- OblivionManager.applyOblivion(ServerPlayer caster, LivingEntity target, int tier) : boolean
- OblivionManager.restoreAll(LivingEntity target) : void
- OblivionManager.stealTrait(ServerPlayer caster, LivingEntity target, ResourceLocation traitId) : boolean
- AbilityMap.getAbilities(EntityType) : List<ResourceLocation>
- AbilityMap.isBoss(EntityType) : boolean
- TraitRegistry.getTraits(EntityType) : List<ResourceLocation>

【实现要求】
1. ⚠️ 全项目最大技术难点：GoalSelector.removeGoal() 是**永久**的
   - P0 的 7 个生物用 Mixin 拦截（干净）或 remove + 保存原 goal 列表 + restore（安全）
   - 其余生物用通用降级：施加一个禁止攻击的自定义 MobEffect
   - 具体方案见 03 §7.3，**先跟集成者确认走哪个方案再动手**
2. BOSS 完全免疫"剥夺"，改为数值化削弱（-20% 攻击力 30 秒）
3. 三张映射表要**逐条落地**，不是抽样
4. 状态必须能正确清理（生物死亡、区块卸载、服务器重启）

【验收标准】
1. ./gradlew build 通过
2. 对至少 5 个不同类型的生物测试遗忘，AI 行为确实被移除
3. 遗忘结束后能力恢复正常（如果是临时遗忘）
4. 对 BOSS 使用遗忘 → 完全无效，改为攻击力削弱
5. 服务器重启后没有残留的"被永久削弱"的生物

【禁止】
- 不要改 spell/ 下的文件
- 不要在没确认方案的情况下动 Mixin（可能影响全局）
```

---

## 十一、Phase 3 · WS-F 卷轴与法书

```
【任务】
实现忆海卷轴物品与 5 个等级的法书。

【必读】
1. docs/tech/10_多线程开发规范.md §〇 §一
2. docs/tech/05_卷轴与法术书.md §1 §3 §4  —— 你的主文档
3. docs/07_装备与道具.md  —— 法书的设计意图

【你拥有的文件】
- src/main/java/com/<author>/mnemosyne/item/MemoryScrollItem.java
- src/main/java/com/<author>/mnemosyne/item/MemorySpellBookItem.java
- src/main/java/com/<author>/mnemosyne/events/InscribeEvents.java

【接口契约】
- MemoryScrollItem extends Item implements IScroll
- MemorySpellBookItem：5 个等级（T1~T5），槽位 3/5/6/8/8
- InscribeEvents：监听 InscribeSpellEvent，做忆格容量校验

【实现要求】
1. ⚠️ 先实测确认：官方 irons_spellbooks:scroll 对忆海法术显示什么贴图？
   - 如果显示默认米黄 → 用自注册的 MemoryScrollItem（本任务）
   - 如果显示正确 → 本任务可以简化（只做法书）
2. 卷轴用 ISpellContainer.createScrollContainer(spell, level, stack)
3. 法书用 ISpellContainer.create(maxSlots, mustEquip, isSpellWheel)
4. 改写容器必须走 mutableCopy() → 改 → toImmutable() → set()
5. getCreatorModId 要返回 mnemosyne（让 tooltip 显示正确）
6. 法书的忆格加成**不要**用 withAttribute，而是在 MnemosyneData.getMaxEngrams 里读装备累加

【验收标准】
1. ./gradlew build 通过
2. 卷轴能右键施放、用后消失
3. 5 个等级的法书槽位数正确（tooltip 验证）
4. 忆海卷轴在官方铭刻台里能抄录
5. 稀有度门槛生效（T1 法书抄不了 Legendary）

【禁止】
- 不要改 registry/ModItems.java（物品注册行由 WS-A 写好）
- 不要复制 ISS 的贴图
```

---

## 十二、Phase 4 · 其余工作流

### WS-D3 高阶法术（6 个）

```
读 docs/tech/10_多线程开发规范.md §〇 §一、
   docs/tech/04_法术等级强度表.md §法术 13-18、
   docs/tech/03_法术实现规范.md §3.4（长吟基类）、
   docs/06_法术_高阶_Epic与Legendary.md 的六个法术章节。

然后实现 6 个高阶法术，文件：spell/high/*.java（只此目录）。

注意三个长吟大招的特殊机制：
- 记忆掠夺：吟唱期按 Z 切换窃取目标，HUD 列出全部特性
- 忆海：吟唱期视觉蓄势，领域半径由等级决定
- 千忆归一：吟唱期忆格逐格"点燃"，被打断不消耗忆格但触发冷却

验收：能施放、数值符合 04 号文档、吟唱期机制正确、被打断行为正确。
禁止：改基类 / registry / spell/low|mid/。
```

### WS-I HUD 与客户端

```
读 docs/tech/10_多线程开发规范.md §〇 §一、
   docs/tech/03_法术实现规范.md §十（HUD 渲染）、
   docs/09_美术与音效.md §HUD 设计。

然后实现忆格 HUD，文件：client/**（只此目录）。

要显示：忆格条（3~5 格）、共鸣加成百分比、腐坏倒计时、长吟期选择列表。

验收：HUD 位置正确、数值与服务端一致、长吟时显示选择列表。
禁止：改 capability/ 的数据结构（只读，通过 WS-B 的接口访问）。
```

### WS-J 遗迹方块与实体

```
读 docs/tech/10_多线程开发规范.md §〇 §一、
   docs/tech/06_世界生成.md §4（NBT 里的方块实体）、
   docs/08_建筑_忆者遗迹.md。

然后实现忆碑方块、忆晶簇方块、忆魇（memory_wraith）实体，
文件：block/** entity/**（只此两个目录）。

忆碑行为：右键弹出记忆文本（8 段，见 08 号文档），每玩家一次，用 locked 忆格存结果。
忆魇：遗迹专属怪物，只在该结构刷（spawn_overrides 已配好）。

验收：忆碑能交互且每玩家只一次、忆晶簇能采集、忆魇能生成。
禁止：改 registry/ 下的注册行（由 WS-A 写好）。
```

### WS-E2 BOSS 免疫与降级

```
读 docs/tech/03_法术实现规范.md §7.4（通用降级）、
   docs/02_核心机制_遗忘与能力映射.md §BOSS 免疫。

然后实现遗忘系统对 BOSS 的处理，文件：oblivion/BossImmunity.java（只此一个）。

要求：BOSS 完全免疫遗忘/窃取/复现，改为 -20% 攻击力 30 秒。
验收：对 BOSS 用遗忘 → 无效但显示"记忆无法被夺走"提示，攻击力确实下降。
禁止：改 OblivionManager 的接口。
```

---

## 十三、集成检查点提示词

**每个 Phase 末尾用一次。这是单线程任务。**

```
【任务】
集成检查点 <N>：合并 Phase <N> 的所有工作流产出，确认接口一致、能编译、能跑通冒烟测试。

【必读】
1. docs/tech/10_多线程开发规范.md §〇（三条铁律）§三（依赖图）
2. docs/tech/01_ISS官方API参考.md  —— 核对接口是否被改坏

【你要做的】
1. 跑 ./gradlew build，修复编译错误
   - 如果是"接口漂移"导致的（某人改了签名），记录下来并在报告里点名
2. 检查文件所有权：有没有工作流改了不属于自己的文件（用 git diff 核对）
3. 检查接口契约是否与 docs/tech/ 里登记的一致
4. 跑冒烟测试：
   Phase 1: 进游戏 → 学派/属性/物品存在
   Phase 2: 生成遗迹 → 写入忆格 → 30 秒后检查
   Phase 3: 拿到 12 个卷轴逐个施放
   Phase 4: 完整流程（探索遗迹 → 学法术 → 战斗 → 遗忘敌人）
5. 输出报告

【报告格式】
## 集成检查点 <N> 报告
### 编译结果：<通过/失败>
### 接口漂移：<无 / 列出>
### 文件所有权违规：<无 / 列出>
### 冒烟测试：<逐项结果>
### 阻塞 Phase <N+1> 的问题：<列出>
### 结论：<可以进入 Phase N+1 / 需要返工>
```

---

## 十四、常用速查提示词

### 只想知道某个 API 怎么用

```
读 docs/tech/01_ISS官方API参考.md，回答：<问题>。
只引用文档里的实测结论，不要用你的先验知识补充。如果文档里没有，明确说"文档未覆盖"。
```

### 想验证一个技术假设

```
读 docs/tech/01_ISS官方API参考.md §包可见性 和 docs/tech/00_技术总览.md §8 个坑。

然后帮我验证这个假设：<假设>

方法：用 javap 反编译 ISS 的 <类名>，把实测结果告诉我。
如果假设不成立，给出替代方案。
```

### 想加一个新法术

```
读 docs/tech/10_多线程开发规范.md §一（确认新法术归哪个 WS）、
   docs/tech/04_法术等级强度表.md（数值格式）、
   docs/tech/03_法术实现规范.md §3（选基类）。

然后按 docs/tech/04_法术等级强度表.md 的格式，给出新法术 <名字> 的完整 5 级数值表，
不要写代码，只要数值表 + 基类选择建议。
```

---

## 十五、与其他文档的接口

| 本文档内容 | 关联文档 |
|---|---|
| 规范的完整说明 | `10_多线程开发规范.md` |
| 各工作流要读的技术文档 | `00` ~ `09` |
| 环境搭建 | `07_1.20.1Forge模组开发指南.md` |
| 法术数值来源 | `04_法术等级强度表.md` |
| 音效事件名 | `08_音效方案.md` §3 |
| 美术生成命令 | `09_美术资源管线.md` §四 |
